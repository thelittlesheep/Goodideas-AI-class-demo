# 這是 demo 用的環境變數範本，提供給 Week 4 PreToolUse hook 實測阻擋用
# 實際專案不會把真正的密鑰放在 repo 中
#
# 任何對 .env.example（或任何 .env*）的寫入都會被 scripts/protect-files.sh 擋下
# 要修改請走 PR + 人工 review

# PORT=3000
# DATABASE_URL=...
# API_KEY=...
